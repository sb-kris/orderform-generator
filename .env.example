# Quill runs entirely in the browser and requires no environment variables,
# API keys, or backend services. This file exists as a placeholder so the
# convention is clear: if server-side integrations (e.g. an e-signature
# provider) are added later, document their variables here with placeholders.
